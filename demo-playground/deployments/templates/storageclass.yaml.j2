kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: mayastor-{{ protocol }}
parameters:
  # Set the number of data replicas ("replication factor")
  repl: '{{ replicas }}'
  # Set the export transport protocol
  protocol: '{{ protocol }}'
provisioner: io.openebs.csi-mayastor
